import type { Config } from "tailwindcss";

export default <Config>{
    theme: {
        extend: {
            colors: {
                primary: "var(--blue)",
            },
        },
    },
    plugins: [],
};
